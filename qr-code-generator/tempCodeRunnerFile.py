        # # Box size
        # ctk.CTkLabel(params_frame, text="Box Size: ").grid(row=1, column=0, sticky="w", padx=10, pady=5)
        # self.box_size_entry = ctk.CTkEntry(params_frame, width=100)
        # self.box_size_entry.insert(0, str(self.default_values["box_size"]))
        # self.box_size_entry.grid(row=1, column=1, padx=10, pady=5)

        # # Border size - corrected label
        # ctk.CTkLabel(params_frame, text="Border Size: ").grid(row=2, column=0, sticky="w", padx=10, pady=5)
        # self.border_entry = ctk.CTkEntry(params_frame, width=100)
        # self.border_entry.insert(0, str(self.default_values["border"]))
        # self.border_entry.grid(row=2, column=1, padx=10, pady=5)
